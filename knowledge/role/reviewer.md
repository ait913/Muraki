# Reviewer の職業的習慣ノート

召集されたら最初に読む。**過去の自分 (歴代 Reviewer) が実際に踏んだ穴**であり、予防的な網羅ルールではない。新しく踏んだら自分で追記・置換する (INDEX 対象外、再生成不要)。

- **validation エラーの assert は status 中心 (400) にする。`error.code` 等の strict assert は zValidator が生 ZodError を返す構成で偽陰性になる。** gotcha 化済みなのに 2 回踏み直した (sessions/2026-06-02)
- **jsdom + Vitest は localStorage / matchMedia 未提供。**先に polyfill (gotcha/jsdom-no-localstorage-in-vitest.md)
- **バグ修正系のレビューは negative control を標準にする**: 修正前コードに戻してテストが落ちることを確認してから GREEN を出す。「テストが実装に迎合していないか」をこれだけが証明できる (sessions/2026-06-11 で有効)
- **偽 RED / テスト全滅の疑いは、実装コードでなく本番経路の直接プローブで切り分ける。** bulk API 全滅に見えた失敗を fixture の日付規約ミスと特定した実績 (sessions/2026-06-11)。fixture の日付は本番の正規化規約 (JST midnight) に合わせる (gotcha/api-test-date-fixtures-must-match-production-normalization.md)
- **Codex へのテスト生成依頼も同期実行・`< /dev/null`** (Developer と同じ穴)
- **既存の失敗テストを「ベースライン」と報告する前に、PJ の known-failures 台帳と照合する。台帳にない失敗は「未分類」と明記して返す** (「ベースライン」の山が本番バグを隠した実績: sessions/2026-06-11)
- **MCP ツールレベルのテスト (go-sdk) は Codex の「設計doc単独生成」では作れない。** ツールは ctx 経由で identity 注入され、ハンドラは unexported。既存の package-internal 統合テスト (例 dandan-app `internal/auth/mcp_integration_test.go`) の `issueAccess`/`testLogin`/`bearerRT`/`StreamableClientTransport` 往復パターンを流用し、Reviewer が同パッケージ内 (`package auth`) で自作する。`mcp.NewServer(Deps{GitHub: 実Client with WithApp+WithAPIBaseURL(fake)})` を auth 保護 `/mcp` の背後に配線 → アクセストークン発行 → `session.CallTool` で往復。IsError メッセージは `res.Content` の `*TextContent`、成功値は `res.StructuredContent` を json 経由で decode。フィールド名は設計doc の戻り値記述から取り、具体値 assert が通れば wire 契約一致の証明になる (sessions/2026-07-04 dandan Slice2)
- **Bash heredoc は中身に `&&` が 1 箇所でもあるとグローバル `block-bash-amp` フックに当たる。** Go コード (→ネスト if に分解、`||` は当たらない) だけでなく、Codex への指示文の散文や「このノート自体の更新」でも発火する (2026-07-08 に 2 回踏み直し)。heredoc を書く前に中身から `&&` を排除する。どうしても文字列が要るときは Python 側で `'AND'*2` 的に結合して生成する (sessions/2026-07-04)
- **`codex` の実体は `~/.local/bin/codex`** (2026-07-16 実測。`/Applications/Codex.app/...` は**存在しない** — 旧記載は陳腐化していた。Codex アプリは `/Applications/ChatGPT.app/Contents/Resources/codex` に同梱もされている)。`command not found` で止まらず `which codex` で引く
- **Codex の sandbox は localhost DB 接続を拒否する** (workspace-write でも network 不可)。Postgres 前提テストは Codex にはコンパイル確認までしかさせられない。実行は Reviewer が自分で回す (TEST_DATABASE_URL + docker postgres を自前起動)
- **設計docは wire/DB 状態を規定するが store API の意味論 (戻り値・入力 struct のどのフィールドを永続化するか) は規定しない。** ブラインド生成テストが store の戻り値や `CreateX(struct{RevokedAt:...})` の永続化を仮定すると偽陽性 fail になる (dandan Slice1 で 2 件: RotateRefreshToken の戻り値 = 更新前スナップショット / CreateMCPToken が RevokedAt を無視)。fixture は直接 SQL UPDATE、assert は DB 直読に倒す。切り分けは probe テスト 1 本 (Create → SELECT) で 1 分
- **redirect の query 値は URL エンコードされて返る** (`rd=/dashboard` → `rd=%2Fdashboard`)。設計docの文字面と Location 文字列を直接比較しない。`url.Parse` → `Query().Get()` で比較
- **「write が起きない」系の失敗で「未配線」と断定しない。** ブラックボックスでは「未配線」と「配線済みだが best-effort `_ =` がエラー握りつぶし」を区別できない (dandan Slice1 で後者を前者と誤帰属しかけた。真因は SQLSTATE 42883 の無音化 → gotcha/best-effort-write-swallows-sqlstate-errors.md)。帰属は観測レベルで書く
- **typed AddTool の入力 schema は非ポインタ全フィールドが required になる (jsonschema-go)。設計docの入力表で `?` が付かないフィールドは required とみなし、ブラインドテストでも常に全フィールドを渡す** (空配列含む)。dandan Slice2 で submit_plan の acceptance_criteria/tags/insights/relations と apply_assignments の reason を省略して 7 テストが偽 fail、1 ラウンド浪費 (2026-07-05)
- **空状態契約 (空配列で返す) の検証は「キー存在 + 値が nil でない + len==0」の 3 点セット。** Go の nil slice は wire で null になり、典型的な系統的違反 (gotcha/go-nil-slice-null-breaks-mcp-empty-state-contract.md)。契約違反は Fatalf でなく Errorf + 空 slice 返しで続行すると、null の裏の挙動 (イベント記録・ガード等) まで同ランで検証できる (2026-07-05)
- **共有 TEST_DATABASE_URL で `go test ./...` するとパッケージ並列 (デフォルト -p) で TRUNCATE 同士が deadlock する (SQLSTATE 40P01)。** 実装バグと誤帰属しない: 単独パッケージ実行 → 全 green なら環境依存。判定用の全体回帰は `go test -p 1 ./...` で取る (dandan 2026-07-05)
- **Codex 生成テストの偽 fail は、まず fixture の合成データ論理を手で追う。** 剰余条件で seed を分岐させるループ (`i%2` で reassign、`i%5` で unassign 上書き等) は、Codex 自身が上書き条件を忘れた assert を書く (dandan Slice3: i=55 は %5 上書きで from=m1 なのに「m1 非関与」と誤 assert。件数 assert は DB 突合で pass しており実装は正しかった)。負系の文字列 assert は「その行が条件に該当しないこと」を seed 式から再計算してから直す (2026-07-05)
- **seed ヘルパの UpsertUser は OAuth ログイン済み user の github_login / github_token_enc を上書きする。** ログイン後に同 user_id で seed すると sealed token が壊れ GitHub 系表示が縮退し、login 文字列 assert も外れる。表示の login assert は固定文字列でなく DB 直読 (`SELECT github_login FROM users WHERE id=$1`) と突合する (2026-07-05)
- **コピー契約 (fidelity 優先) の PJ では、設計docの検証条項がコピー元原文と衝突したらコピー元が勝つ。** dandan Slice2 で 2 件: §9.5-2「存在しない grep 0 件」は天野原文 description「存在しない ID はスキップ」に誤ヒット (意図は policy/open_* の先回り文言のみ)、0 件応答の空配列契約は天野が nil slice (wire null) を返すため null が正。判断前に許可されたコピー元比較で原文を突合してから assert を絞る (2026-07-08)
- **ダッシュボード HTML の負系 grep (「この名前が出ないはず」) はフィルタ UI の select 選択肢に誤ヒットする。** フィルタの正しさは data-testid 行数一致で取る (dandan 2026-07-08)
- **MCP Apps UI ハーネス (report-back 方式) の駆動は「hostLog にリクエストが載った」で先へ進まない。** hostLog は送信記録で応答適用を保証しない → 応答適用後にしか変化しない DOM マーカーを待つ。ui/message の text は `params.content[0].text` (`params.text` は常に undefined)。dark 検証は `--force-dark-mode` + 実際に色の付く要素の dark palette 値。詳細 gotcha/mcp-apps-ui-harness-testing.md (2026-07-08 Slice3 で 6 シナリオ偽 fail)
- **設計docの「friendlyError 契約」等の UI 横断記述は、コピー契約 PJ では画面ごとに実装有無を原文で確認してから assert する。** 天野 friendlyError は workspace.html のみ (insights.html は JSON.stringify で Error→"{}" 表示)。note の「コピー元が勝つ」原則の画面単位 instance (2026-07-08)
- **dev の `.env` が Vitest に漏れて `.env.test` を上書きする (app が import 時に dotenv で `.env` を読む構成)。2026-07-14 と 2026-07-17 で 2 回踏んだ。** 漏れるのは 1 変数でなく **3 変数 / 計 5 件** (`BETTER_AUTH_COOKIE_DOMAIN` → `auth [§8 #7]` / `PUBLIC_WEB_URL` → `cors-cookie [§8 #70]` / `BETTER_AUTH_TRUSTED_ORIGINS` → `ios-api §8.4` 3 件)。**~~「.env を正しい値に直すと全 pass」~~ は誤った処方だったので撤回する**: dev の `localhost` / `localhost:5173` は **dev としては正しい値**で、直すとローカル開発が壊れる。真の欠陥は値でなく**漏れること自体**。**切り分け**: 失敗テスト内で `console.log(process.env.THAT_VAR)`。**検証で書き換えたら `cp` バックアップ → `diff` でバイト単位復元する** (dev 値は正しいので直した状態で放置しない)。env 由来の一括失敗は「CORS が壊れた」「認証が壊れた」の顔で現れ、**新 feature の regression と誤帰属しやすい** — 帰属は negative control (feature を消した状態で同じテストを回す) で決める。詳細 gotcha/dev-dotenv-leaks-into-vitest-overriding-env-test.md (2026-07-17)
- **~~prisma CLI がこの環境で無言ハングする~~ → 解消済み (2026-07-17 実測)。** 真因は iCloud evict (memory `env_icloud_evict_hangs` 参照) で、`prisma --version` / `prisma generate` とも正常動作する。**シムで差し替える回避策はもう要らない。** ただし `pnpm install` 後は `pnpm exec prisma generate` が必須 (postinstall フック無し)。忘れると `@prisma/client does not provide an export named 'PrismaClient'` — コードのバグに見えるが未生成なだけ。**なお macOS に `timeout` は無い** → `perl -e 'alarm N; exec @ARGV'` を使う
- **同一 sqlite tmp を共有する Vitest を 2 プロセス並行起動するとロックでハングする** (singleFork + 共有 DATABASE_URL)。ハング時はまず `pkill -f vitest` して 1 本ずつ回す。harness が長時間コマンドを勝手に background 化するので、前の run が生きたまま次を撃たない (2026-07-14)
- **env 由来の分岐 (provider config, trustedOrigins 等) は「プロセス起動前に env を投入」して検証する。** `env.ts` が import 時に一度パースして固定する構成では、実行時 `process.env.X=... → resetAuth()` は届かない。設定側の検証は APPLE_* を export した状態で 1 本だけ回す boot-probe に回し、未設定側 (404) だけ in-process で見る。詳細 gotcha/env-module-import-time-parse-defeats-runtime-env-swap.md (2026-07-14)
- **better-auth 1.6.11 は magic-link の verification.identifier を平文 email でなく不透明ランダムトークンで保存し、verify 失敗を 302 リダイレクト (JSON 400 でない)、送信失敗を 5xx に伝播しない。** 旧 auth.test.ts の `identifier contains email` / verify 400 / resend 失敗 500 期待は陳腐化。send 自体 (200 + verification 1 行 + resend 1 回) は正常。バージョン更新後の「既存が赤い」は挙動変化をまず疑い、probe で実挙動を採取してから陳腐化と断ずる (2026-07-14)
- **インフラ設計 (Dockerfile/workflow) のレビューは「ユニットテストが無いから回せない」で終わらせない。** 設計docが「docker daemon が無い」と書いていても**自分で `docker version` を確認する**。omatase では Docker Desktop が `open -a Docker` で起動でき、K1-K8/H4/H5 を全て実走できた (Architect 環境の不在をそのまま引き継がない)。CI job の shell も yaml から `yaml.safe_load` で `run` を抜き出せば**手元で実行できる**: fake API サーバ (python http.server) を立てて `COOLIFY_API_BASE` を向ければ D1-D8 のような「本番でしか出ない」分岐が全部踏める。ポーリングの長い timeout は**コピーを sed で短縮** (`seq 1 180`→`seq 1 3`) して経路だけ確認する (2026-07-16)
- **healthcheck の検証をホスト側のポート公開プローブで代用しない。** `docker run -p 18080:3000` + `curl 127.0.0.1:18080` は 0.0.0.0 bind なら通るが、Coolify の healthcheck は**コンテナ内から localhost** を叩く。Node の `HOSTNAME=0.0.0.0` は IPv4 のみ bind し、busybox wget は localhost を ::1 で引くため落ちる (Go は dual-stack なので通り、非対称が誤帰属を生む)。`docker exec` で実 CMD-SHELL を実行して初めて検出できた。詳細 gotcha/coolify-healthcheck-localhost-ipv6-vs-node-bind.md (2026-07-16)
- **「実装が設計に忠実」と「本番で動く」は別。** 設計docのシェル片をそのままコピーした実装は diff 上は完璧でも、設計自体の欠陥 (D2 の `resp=$(curl --fail-with-body ...)` は set -e で body を echo する前に落ちて**ログに body が出ない**) をそのまま継承する。失敗の帰属先は「実装 or 設計」であって、実装が設計通りでも**設計の不備なら報告する** (2026-07-16)
- **自分のテストハーネスのバグを実装のせいにしない。** 2026-07-16 に 3 回踏んだ: (1) fake サーバの readiness を待たず `sleep 2` で撃って偽 fail、(2) readiness 判定に `[ "$c" != "000" ]` を使い、curl の `000` + `|| echo 000` で `000000` になって即 break、(3) fake API のルーティングで `/api/v1/deployments/x` が `startswith("/api/v1/deploy")` に誤ヒットし全 status が null。**偽 fail の第一容疑者は自分のハーネス**。「全シナリオが同じ異常値」を見たらルーティング/起動を疑う
- **負のコントロールは「自分で手打ちしたコマンド」でなく `yaml.safe_load` で ci.yml から抜いた実 step を bugged 対象に当てる。** 手打ちだと「設計通りのコマンドは検出できる」しか言えず、**実装された step が検出するか**は未証明のまま。omatase では step の `run` を抽出 → `${{ matrix.* }}` を置換 → `docker exec probe` が要るので同名コンテナを bugged (`-e HOSTNAME=0.0.0.0`) で立てて実行 → exit 1 を確認、で「CI が実際にバグを捕まえる」を証明できた (2026-07-16)
- **「この matrix entry でも検出できるか」を対象アプリの都合で諦めない。** Go は IPv4-only bind を強制する env が無く backend の検出能力を直接示せなかったが、**別イメージ (web) を `-e PORT=8080 -e HOSTNAME=0.0.0.0` で立てて backend 用 step を当てる**と「localhost:8080 の IPv4-only listener を step が exit 1 で捕まえる」が直接示せた。検出能力の証明に必要なのは「その port でその症状が出ている listener」であって、本物のアプリではない (2026-07-16)
- **`git show <base>:<file>` で修正前の実装を取り出して当てるのが、修正系レビューで最も安い negative control。** omatase D2 は旧 step (775c08d) を fake API に当てて「body が消える」を再現し、新 step との差を 1 コマンドで示せた。worktree を汚さずに済む (2026-07-16)
- **`curl ... || echo 000` は `-w '%{http_code}'` の `000` と重なって `000000` になる。** 自分のハーネスでこれを踏んだ (note 済) だけでなく、**レビュー対象のシェル片にも同じ形が現れる**。`= "200"` 比較なら無害なので「バグ」と報告しない。`!= "000"` 比較をしている箇所だけが本当の欠陥 (2026-07-16)
- **フォント/画像など「フォールバックが親切な API」の検証に、そのフォールバックする API 自身を使わない。** `Font.custom` は解決失敗を無言でフォールバックするので登録漏れを検出できない (atender は全画面 SF Pro のまま TestFlight に出ていた)。`UIFont(name:)` は nil を返す → こちらで assert。詳細 gotcha/swiftui-font-custom-silent-fallback-hides-missing-uiappfonts.md (2026-07-16)
- **ベースライン件数の台帳は「測った日 + 測ったコミット + failure 数」まで書かせないと failure を隠す。** atender の「iOS 157 GREEN」は実測 172 テスト / 2 RED で二重に誤りだった。件数だけの台帳を信じると「総数が合ってる」で RED を素通りする。**台帳と実測が食い違ったら、台帳でなく実測を正とし、その場で台帳を置換する** (2026-07-16)
- **`pnpm exec tsx` で API を起動する系は、この Bash-tool 環境では無言で起動しない** (プロセスは alive だが listen せずログもゼロ。`( … & )` / `nohup` / サンドボックス無効 いずれも不可)。**前景 `perl -e 'alarm N; exec @ARGV'` でも出力ゼロ**なので原因を追う手がかりが無い。prisma を直接叩く seed スクリプトは動く。**API 前提の E2E/sim 検証は撃たずに Leader へ「未検証」と返す方が安い**。撃つと停止できない tsx プロセスが溜まる (`pkill` は permission denied で掃除もできない) (2026-07-16)
- **「純粋ロジックはテストできるが View の `.task` 配線はテストできない」を GREEN で誤魔化さない。** `HomeChips.items` が全 pass でも「温キャッシュ時に rooms が読まれるか」は 1 ミリも証明していない。純粋関数の GREEN が配線の GREEN に見える構造なので、**カバーできていない挙動は判定に明示的に書く** (2026-07-16)
- **`DecodingError.keyNotFound` の localizedDescription は "The data couldn't be read because it is missing." で、**空データの decode と同じ文面**。** `APIError.decoding(...)` のように localizedDescription だけを包む型だと、どのキーが無いか消える。この文面を見て「body が空 = スタブが効いていない」と誤診し 1 ラウンド浪費した (2026-07-17 atender)。切り分けは**同じ経路を最小 JSON で叩くプローブ 1 本** (通れば配線は生きており、犯人は fixture のキー漏れ)。**そもそもスタブ JSON を手打ちしない** — 稼働中の実 API から curl で採取して fixture 化すれば起きない
- **リーダーから渡る「契約仕様」の型名・シグネチャは実在確認してから使う。** atender で「`RoomWeekRepository.roomWeek(id:weekStart:force:)`」と渡されたが実在せず (実体は `RoomRepository.roomWeek`)、`cannot find in scope` で足止め。**実装を読まずに名前だけ引く手段: ビルド済 `.swiftmodule` を `strings` して型名/メソッド名を拾う** (`strings Atender.swiftmodule/arm64-*.swiftmodule | grep -oE "[A-Za-z]+Repository"`)。ロジックは見えず、呼び出しに必要な名前だけが得られる。init のアリティは**コンパイルエラーに教えさせる** (`has no member 'members'` → `roomMembers` と判明)。これで「実装コードを読まない」を守ったまま blind test を書ける (2026-07-17)
- **Vitest の in-process テストは CJS/ESM interop バグを原理的に検出できない。** Vite の transform は Node native ESM より寛容な interop shim を挟むので、`import * as ical from "node-ical"` が **Vitest では動き `tsx`/`node dist/` では `parseICS is not a function`** になる。atender の ICS インポートは**この穴に落ちて「一度も動いたことがない機能」が出荷されていた**。ブラインド生成した 5 本は**修正前コードでも 5/5 GREEN** (負のコントロールで発覚)。→ CJS パッケージ (`node-ical`/`rrule`/`jschardet`/`iconv-lite` 等) の interop は **`execFileSync(node_modules/.bin/tsx, [probeFixture])` の子プロセス probe テスト**でしか担保できない。詳細 gotcha/vite-cjs-esm-interop-hides-node-esm-failures.md (2026-07-17)
- **「機能が一度も動いたことがない」バグは vacuous test を量産する。** 全経路が同じエラーで落ちていた場合、**負系テストが壊れたコードでも通る** (atender: 修正前は valid ICS も garbage ICS も等しく `400 INVALID_ICS` → 「ゴミは 400」を status だけで assert すると修正前でも緑)。→ 負系は **「その 400 が本来の理由で出ているか」までメッセージで assert** する (`expect(msg).not.toMatch(/parseICS is not a function/i)`)。正系も status でなく実値 (parsedCount / start / end) まで見る (2026-07-17)
- **負のコントロールが「赤くならなかった」は成果であって失敗ではない。** テストが無力だと**証明できた**ということ。そこで「まあ緑だしヨシ」にせず、**実ローダを踏む別レイヤのテストを新設して赤→緑を示す**まで行く。role note 7 の「負のコントロールを標準にする」はこの日、実際に誤 GREEN 報告を 1 件防いだ (2026-07-17)
- **手元の TZ に寄生した緑を疑う。** dev は JST、`node:20-alpine` 本番は **UTC**。ICS の終日 (`VALUE=DATE`) のような floating date は**プロセス TZ で UTC 実体が変わる** (JST→`07-08T15:00Z` / UTC→`07-09T00:00Z`)。`TZ=UTC pnpm exec vitest run` を 1 回撃つだけで炙り出せる。**Codex が生成した日付 assert は絶対時刻ベタ書きになりがち**なので必ずこれを通す。詳細 gotcha/ics-all-day-floating-date-depends-on-server-tz.md (2026-07-17)
- **wire 契約 (multipart のフィールド名・レスポンス shape) は実装を読まずに「動いている API へのブラックボックス probe」で採れる。** atender では稼働中の localhost:8787 に空 POST → `{"code":"FILE_REQUIRED","message":"file field required"}` でフィールド名が判明、探索用の使い捨て probe テスト 1 本で 201/preview の shape が全部採れた。**Codex に shape を渡してから書かせる**と偽 fail のラウンドが消える (role note 45 の「名前だけ引く」の HTTP 版) (2026-07-17)
- **負のコントロールが「赤くならない」ときは、テストの無力を疑う前に「変異が届いたか」を疑う。** atender の 426 傍受は実装に **3 site** あり、**2 site が `status == 426` / 1 site が `httpResponse.statusCode == 426`** と表記が割れていた。設計docの文字列から導出した変異 (`status == 426` を置換) は 2 site にしか当たらず、`upload` 経路のテストが緑のまま → 「vacuous だ」と誤診しかけた。3 site 全て殺したら 4 本が RED になり、テストは実経路を見ていた。**変異前に `grep -c "<リテラル>"` で site 数を数え、`re.subn` の置換数と突合する** (実装のロジックは読まずに済む: `grep -o "[a-zA-Z.]* *== *426\|case 426" | sort | uniq -c` で「形」だけ列挙)。設計docが「N メソッド全てで」と書くなら **site 数 = N の一致自体が設計忠実性の証拠**になる。詳細 gotcha/mutation-must-be-proven-to-reach-all-sites.md (2026-07-17)
- **`git worktree add <path> main` は main が別 worktree で checkout 済みだと fatal。** `--detach` を付ける。feature の regression 判定に要るのは main でなく**ブランチの分岐元** (`git merge-base main HEAD`) であり、worktree を作らずとも `git show <merge-base>:<file> > <file>` → テスト → 復元、が最も安い。**★ ただし復元に `git checkout -- <file>` を使ってよいのは、その file が作業ツリーでクリーンなときだけ。** Reviewer が受け取る worktree は Developer の未コミット変更を持つのが常で、`git checkout --` は**変異前でなく HEAD に戻す** = **Developer の実装を無言で消す** (2026-07-17 atender P1 で実際に `project.yml` の feature を消した)。**復元は常に `cp` バックアップ → `diff`/`md5` でバイト検証**。詳細 gotcha/git-checkout-restore-destroys-uncommitted-work.md
- **負のコントロールが「一部のテストだけ赤くする」とき、赤くならなかった 1 本を必ず個別に調べる。** atender P1 の JST→UTC 変異は SchoolClockTests 6 本中 5 本を殺したが `#C5 displayDay` だけ無傷だった。テストが無力なのではなく、**Codex が標本時刻に 12:00 を選んでいた**から (JST 12:00 = UTC 03:00 で曜日が変わらない)。**TZ シフトの変異は、シフト窓の中に標本が無いと原理的に検出できない。** displayDay は §5.3 の「今日の列」を決める関数で、バグの症状 (「月曜の 9 時前に先週を読む」) はまさに 00:00〜08:59 に出る = **最も危険な窓が唯一の無検出窓だった**。08:00 / 00:00 / 00:30 の標本を足したら変異が 7 (日) を返して即赤に。**Codex の日付・時刻 assert は「安全な正午」に寄る**ので、境界窓の標本は Reviewer が自分で足す (2026-07-17)
- **設計docの逐語コード片が、それ自体 strict concurrency 違反のことがある。** atender §3.6 の `ScreenMetrics.height` は nonisolated な static var から `UIApplication.shared` (= `@MainActor` 隔離) を触る形で書かれており、**逐語実装すると actor 隔離警告 4 件**。Developer が `@MainActor` を足したのは逸脱でなく**設計の欠陥の是正**。判定は「逐語か否か」でなく**契約が保たれたか**で下す: 値の契約 (doc の式を test 側で再構成して突合) と**呼び出し契約**を分けて測る。呼び出し契約の変化は**使い捨ての compile プローブ 1 本**で出る (nonisolated な XCTestCase から呼んで `main actor-isolated ... can not be referenced from a nonisolated context` が出るか) (2026-07-17)
- **「削除したはず」の API が本当に消えたかは、コンパイラに聞くのが最短で、実装を読まずに済む。** 使い捨てプローブに削除対象 5 つを並べて `build-for-testing` → `type 'X' has no member 'y'` が 5 件出れば削除は証明済み。grep だとコメントや別名に誤ヒットするが、コンパイラは嘘をつかない。逆に**通ってしまったら設計違反が確定**する。プローブ削除後は `xcodegen generate` を忘れない (xcodeproj が消したファイルを参照し続けて `Build input file cannot be found` になる) (2026-07-17)
- **`plutil -extract` は `-o -` を省くと入力ファイルを抽出結果で上書きする** (既定の出力先が stdout でなく input file)。atender P1 で Info.plist を `["GoogleSans-Medium-Latin.ttf"]` だけの中身に化けさせ、直後の `xcodebuild test` が `unable to read property list` で **TEST FAILED**。**値は表示されるので壊した自覚が無く、危うく「実装が壊れている」と報告しかけた** (role note 35「偽 fail の第一容疑者は自分のハーネス」が 2 度目に効いた)。読むだけなら `-o -` / `PlistBuddy -c Print` / `grep`。Info.plist は xcodegen 生成物なので `xcodegen generate` で復旧可。詳細 tool-quirk/plutil-extract-overwrites-input-file.md (2026-07-17)
- **「目視で判定せよ」と設計が名指しした*その*スクショが、その性質を映せないことがある。** atender §10.1 は「`01-home-timetable` のタブバーが**背後のコンテンツが透けている**こと」を要求したが、**その画面はタブバー背後が空白**だった (P3 が消す予定の `SelfTodayCTA` の `tabBarHeight` パディングがコンテンツを押し上げているため)。透過は**背後にコンテンツがある画面** (`C01-semester-overview`: カレンダー行と凡例がガラス越しに屈折して見える) で初めて観測できた。→ **検証条項の「どの画面で」は鵜呑みにせず、その性質が現れる画面を自分で選ぶ。** 「名指しの画面で見えない」を「実装が効いていない」と誤帰属しかけた (2026-07-17)
- **ピクセル判定は「一致」でなく「2 つの候補値のどちらに近いか」で書く。** ガラス合成は accent を素通しさせない: 同じ azure asset が iOS 18.2 では `(30,150,230)` = `#1E96E6` ちょうど、iOS 26.5 では `(19,139,220)` = **d≈17 ズレる**。`== #1E96E6` で書くと 26 側が偽 RED になる。`d(azure)=18` vs `d(orange)=305` の比較なら退行だけを拾える。**同一ピクセル数 (n=3129) で hue だけ変わった A/B** は、幾何が同一である証拠にもなるので併記する (2026-07-17)
- **UI テストハーネスはデバイス非可搬。「同じフローを SE でも走らせる」は設計 doc が書きやすいが、ハーネスがその移植性を持つ保証は無い。** atender の `ScreenshotFlow` は SE で `ルーム` タブの `isHittable` ゲートに落ちてホームに留まり、`buttons CONTAINS "情報処理科"` が**ホームの ContextChip** に誤ヒット → **`E02-room-detail_ok=true` という名前でホームを撮り**、byte-identical を返した。**`_ok=true` は「その label が存在した」以上を意味しない。** 帰属は **main で同じフローを走らせて一致** (= 既存アーティファクト) で確定した。詳細 gotcha/screenshot-byte-identity-conflates-noop-tap-with-lost-harness.md (2026-07-17)
- **時刻・曜日依存の UI は「今日」が実験日に縛られ、live データでは正の視覚検証ができないことがある。** atender P3 の `NowNextBar` (次の授業バー) は平日のみ出て土日は仕様どおり隠れる。検証日が土曜だと API の `/api/today` が `occurrences:[]` を返し、バーは正しく隠れるが**「平日に出る」ことは live では撮れない** (server は host clock で today を決めるので sim clock を変えても追随しない。demo dev.db は共有なので Saturday クラスを注入するのは residue リスク)。Developer も同じ土曜で撮って「非表示」を出していた。→ **正の視覚は諦め、(a) 土曜で「正しく隠れる」を撮る (b) 状態機械 (`TodayTimeline`/`NowNextText`) の網羅ユニット (#N/#L) が緑、の 2 点で担保**し、カバレッジ穴として明示する。ScreenshotFlow の `06-cta-expanded_ok=false` 等はバー不在でソフトタップが空振りした証跡であって RED ではない (2026-07-18)
- **`0 failures` は月カレンダーの「日番号が隠れる」を 1 ミリも捕まえない (§9.4 の実例)。** atender P3 で `CalendarMonth` を動的高 (`CalendarMonthLayout` min 56) + `prefix(2)` + 11pt に作り替えた結果、**1 日に event chip が 2 個ある日 (デモの月曜) で「番号 + 2 chip」が行高を超え、隣の週セルに溢れて日番号 (20 等) が上の週の chip に覆われる**。iPhone 16 (大画面) でも出る = SE 限定でない。268 テスト全緑・#CA1-#CA10 も緑。**Touri が名指しした「最も10年前」の画面なので、番号が読めない = ship 不可。** これは「描画は目視」(§9.4) の教訓の実弾: マス描画 (不透明/枠なし/上寄せ) が正しく直っても、**動的高レイアウトの overflow は別軸のバグ**で、スクショのマス目を 1 枚実際に拡大 (`PIL crop`) して番号が生きているか見るまで気づけない (2026-07-18)
- **「host 検証が無い」系のセキュリティ上申は、まず実テストで挙動を実証し、次に「その値が実際にどこへ流れるか」で実害を判定する。** atender QR 招待 (2026-07-21): `QRScanResult.deepLink` が `DeepLink.parse(url) != nil` のみで host を見ないため `https://evil.com/rooms/join/X` が `.roomJoin(code:"X")` を返す (probe テストで実証、lookalike `atender.appily.run.evil.com` も通過)。**だが実害ゼロ**: (1) スキャン URL はブラウザで開かれず path 埋め込みの code だけ抽出される、(2) code はアプリにハードコードされた自 API (atender-api) に送られサーバ検証される — **scan した host はどこにも影響しない**、(3) 攻撃者は正規 host `atender.appily.run/rooms/join/<自分の code>` の QR を作れるので **host allowlist を足しても防御力はゼロ** (code こそが攻撃ベクタで、それは host 非依存)。→ 招待 deeplink の host 検証は「URL がブラウザで開かれない限り」over-engineering。判定の骨: **抽出値の宛先が固定 (自 API) なら scan URL の host は捨てられており、host-check は無意味**。この推論を probe (実挙動採取) → データフロー追跡 (設計 doc の「code は既存 join/add API へ」記述) の 2 段で組む (2026-07-21)
- **「additive な DTO 拡張」は wire では additive でも、既存テストコードには source-breaking なことがある。** atender 公開時間割検索 (2026-07-21): shared/iOS の `TemplateDto` に**非 Optional** な `schoolName`/`departmentName` を足す設計。wire (zod `z.string()` 非 strict / backend が常に出力) は additive で Web も壊れないが、**iOS 側は 2 箇所で既存テストを壊した**: (1) `RoomLogicTests.swift:415` の `TemplateDto(...)` **memberwise init** が 2 引数不足で**コンパイルエラー → AtenderTests target 全体がビルド不可**、(2) 共有 decode fixture `template.json` が新フィールドを欠き、非 Optional decode で `DTODecodingTests:366` が**実行時 throw**。commit はこの 2 つを更新しておらず、268 GREEN ベースラインが壊れていた。**なぜ Developer が気づかなかったか: Codex sandbox は GoogleSignIn の SwiftPM clone がネットワーク制限で落ちるため iOS test target を一度もビルドできない** → コンパイル破綻が無検出のまま出荷される。→ **iOS の DTO に非 Optional フィールドを足す feature は Reviewer が必ず sandbox 外で `build-for-testing` する** (ユニットの pass/fail 以前にコンパイルが通るかが第一関門)。切り分けは `grep -nE "error:" build.log | grep -v "0 errors"` で 1 行。**「additive」という設計語を wire の意味だけで受け取らない**: 非 Optional 追加は memberwise init 呼出し (テスト含む) と decode fixture 全てに波及する
- **共有 decode fixture (`template.json` 等) と B6「欠落 JSON は throw」テストは同じ fixture で両立できない。** 非 Optional を足すと、既存の「成功 decode」テストは fixture 更新が必須になり、「欠落で throw」テストは**別の inline JSON** を使う必要がある (同じ fixture を両方に使うと必ず衝突)。Codex は throw テストに既存 fixture を流用しがちなので inline JSON に差し替える
- **「ユニット検証可能」を約束する前に、その挙動が pure function か private View 文脈かを swiftmodule demangle で確定する。** atender TT fixes (2026-07-21): 友達検索のボタン状態 (T8) は `strings <Atender.swiftmodule> | grep '^\$s7Atender' | xcrun swift-demangle` で見ると `FriendsView.section(...variant: FriendCard.Variant...)` の**private context (`_5491198F...`)** にしか存在せず、`@testable import` でも到達不能 = 手動確認扱いが正しい。逆に invalidation (`invalidationTargets(for:)`) や `FriendshipBuckets.split` は module-level の pure func で即テスト可。**iOS test は 1 ファイルのコンパイルエラーで target 全体が崩れる (build cost 大) ので、blind test を書く前に demangle で「呼べる形か」を確認してから書く。** `Color.toHexString()` は binary に存在確認できても逆変換 `Color(hex:)` の symbol が demangle に出ない (=inlinable/linkage 無しの可能性) なら往復テストは博打 → 見送って報告に明示。demangle が出す func 宣言に無い method は、書いて building して初めて分かる (2026-07-21)
- **Swift enum の `case `self`` は、leading-dot 文脈で `.self` と書くと**メタタイプでなくその case** に解決される。** atender HomeContext (`.self`/`.room(roomId:)`) の blind test で「`.self` は予約語衝突でコンパイル不能では」と疑ったが、`enum E{case `self`}; let x:E = .self` は exit 0 で `x == .self` も真 (2026-07-22 隔離 swiftc で実証)。**内部 enum の case 綴りは abi.json/swiftmodule strings に出ない** (public ABI のみ・cases は non-symbol) ので、設計 doc の綴りが不安なら**3 秒の隔離 swiftc プローブ**で言語仕様として決着させる (test-target 全体の高コスト compile を 1 ラウンド節約)。static enum メソッド (`HomeDrawer.sections` 等) も swiftmodule strings に外部シンボルとして出ないが、app target が緑ビルドで参照している時点で `@testable` 到達は保証される (2026-07-22)
- **role note 63 の削除側**: iOS の「孤児 DTO/enum 掃除」(atender F5 gcal 撤去型) も同じく AtenderTests target を無言でコンパイル不能にする。atender calendar-eventkit (2026-07-23): F5 が `GoogleSyncStatus` 等の Google sync DTO/enum を `DTOs.swift`/`Enums.swift` から削除したが、既存 `DTODecodingTests.swift:406` が `decode(GoogleSyncStatus.self...)` を参照したまま → **TEST BUILD FAILED (`cannot find 'GoogleSyncStatus' in scope`)** で 308 ベースライン + 新規テストが 1 本も走れない。Codex sandbox は GoogleSignIn SwiftPM clone がネット制限で落ち iOS test target を一度もビルドできないので無検出のまま出荷される。**DTO を足す feature だけでなく消す feature も Reviewer が sandbox 外で `build-for-testing` する**。切り分けは `grep -rn "<消したシンボル>" apps/ios/AtenderTests/` で dangling 参照を先に洗う (削除シンボルは設計 doc の「掃除対象」節から取れる)。修正は陳腐化 assert の撤去 (enum が設計上消えたので正しい対処)。build 成功 → `-only-testing` で新規だけでなく full AtenderTests を回して baseline 件数 (308→317) を突合する (2026-07-23)
- **Go の handler blind test は `go doc ./internal/http` で公開シグネチャ (コンストラクタ名・Options/interface のメソッド集合) だけ引けば、実装ロジックを読まずに fake を組んで書ける** (Swift の swiftmodule strings / demangle の Go 版。role note 45/55/66 の系譜)。omatase P0/P1 (2026-07-23): `New(opts Options) http.Handler` と `type Store interface{...}` を go doc で確認 → 全メソッドを持つ in-memory fake を test 内定義 → E1〜E6 の handler/authz を DB 非依存で回せた。Codex にも「assert は設計から・コンパイルのためのシグネチャ確認 (`go doc`) だけ許可・ロジックは読むな」と明示すると偽 fail ラウンドが減る。
- **middleware が constructor の外にある構成では、handler blind test が「wire ヘッダが付かない」で偽 RED になる。帰属は `Options` 等の構成 struct を go doc で見れば 1 分で確定する。** omatase B8 (2026-07-23): Codex の TestCORS が `httpapi.New(...)` の出力に CORS ヘッダを期待して RED。だが `go doc ./internal/http Options` に allowed-origins フィールドが無く、CORS の実体は `OriginChecker.Middleware(next)` (合成ルート main.go が巻く層) だった → **実装バグでなくテストが誤った層を叩いていた**。修正は `NewOriginChecker(...).Middleware(inner)` を直接叩く unit に書き換え (B8 の echo/deny/preflight を実検証)。「constructor が middleware を内包するか」は Options のフィールド有無で判る (origins を受け取らない = 内包しない)。「main.go が実際に巻くか」は合成ルートの配線でありユニット未カバー — 判定に明示する (role note 43)。
- **設計の「body で受け取らない = なりすまし不可」系のセキュリティ文言は、strict reject (400) と silent ignore (200) の両方で満たされる。テストが後者を決め打つと偽 RED になる。** omatase E1 (2026-07-23): 設計「行の user_id は JWT sub で確定 (body で受け取らない)」に対し Codex が「未定義フィールド user_id を混ぜて 200 (無視)」を期待 → 実装は unknown field を 400 拒否で RED。だが**400 拒否もなりすまし不可を満たす** (むしろ厳格)。設計は unknown-field の寛容さを一切規定していない = **設計の曖昧さ / テストの過剰仕様**であって実装バグでない。セキュリティ intent の検証は「正当リクエストで永続 user_id == JWT sub」を見れば足り、smuggle の status を決め打つ必要はない。
- **Codex を『テスト生成』に呼ぶと、失敗したテストを通すために実装 (handler.go 等) を黙って書き換える。** omatase P2P3 (2026-07-23): 設計だけからテストを書けと明示したのに Codex は E9 (limit を max100 に clamp) と E15 (member が todo に status:cancelled を送ると 403) の 2 箇所で handler.go を編集し「設計差分を修正した」と申告。**Developer の E7-E17 は未コミットの作業ツリー**だったため、Codex の apply_patch が Developer の原本を上書きし復元不能になった (role note 52 の git-checkout 破壊と同じ事故を Codex 版で踏んだ)。→ **Codex にテスト生成させる前に、レビュー対象の実装ファイル群を `cp` で退避する** (未コミットが常態)。生成後 `git status`/`git diff --stat` で**テストファイル以外が触られていないか必ず確認**する。触られていたら、その差分は「Developer の実装が設計から外れていた」証拠 (Codex は設計に忠実なテストが落ちた時しか実装を触らない) なので、負のコントロール (その編集を戻して該当テストが RED になるか) で実 deviation を実証してから帰属する。omatase では E9 clamp と E15 cancelled-guard の 2 件が実 deviation と確定 (どちらも設計は一意 = 実装バグ)。Codex の指示文に『実装を1行も変えるな・コンパイル用のシグネチャは go doc だけ』と書いても**守らないことがある**前提で退避する。
- **TS クライアントの blind test に import シグネチャが要るときは `tsc --declaration --emitDeclarationOnly` で `.d.ts` を吐かせる (`go doc`/swiftmodule strings の TS 版)。** omatase web (2026-07-23): `src/**` のロジックを読まずに export 名・型・引数だけを `.d.ts` から採取して Codex に渡せた (module 解決エラーが出ても declaration は emit される)。Codex に「この .d.ts のシグネチャだけ import してよい・期待値は設計から起こせ・実装は 1 行も変えるな」と渡すと偽 fail ラウンドが減る。生成後は `md5` で実装ファイルの無改変を backup と突合 (role note 71 の Codex 実装書き換え対策。今回は committed だったので git 復元も可)。
- **api client のエラー写像は status だけでなく `code`/`message` の実値まで assert する。** omatase web (2026-07-23): REST 正典が nested `{error:{code,message}}` を規定するのに web の `apiFetch` は top-level `{code,message}` を読んでおり、契約どおりの nested を渡すと全エラーが `unknown_error`/空 message に潰れていた。status(403) だけ見ていたら緑で見逃す (封筒 shape 違いは status に出ない)。帰属は fetch モックの body 形を nested/flat で振る probe 一発で確定 (実装非読)。詳細 gotcha/client-flat-error-read-collapses-nested-error-envelope.md
- **bash 禁止の二重アンパサンド (&&) は heredoc の中の JS/TS コード (`status >= 200` を論理積で繋ぐ式) でも block-bash-amp フックに当たる (role note 12 の再確認)。** probe test を heredoc で書くとき `[a, b].every(Boolean)` 等で回避する (2026-07-23)。
- **再接続 backoff+jitter / visibilitychange / re-fetch が React フック (`useEventSocket`) 内に inline されていると純関数 unit で検出できない。** omatase web (2026-07-23): envelope parse は純関数 export でテスト可だが、再接続ロジックは hook に癒着し export された純関数が無い → 「穴」として報告 (role note 43 の系譜)。Architect/Developer への actionable は「`nextBackoff(attempt): number` 等を pure fn に抽出」。grep で `^export ` を数えれば「フックしか export していない = 癒着」が実装ロジックを読まずに判る。
- **TS の blind test でも「export surface」は tsc で機械抽出できる (go doc / swiftmodule strings の TS 版, role note 45/68 の系譜)。** omatase mobile (2026-07-23): `tsc --declaration --emitDeclarationOnly --ignoreConfig --skipLibCheck --module esnext --moduleResolution bundler --jsx react-native --outDir <scratch> <files...>` で、path alias 未解決 (TS2307) が出ても **.d.ts は生成される** = 関数シグネチャ・型だけが得られ、ロジック本体は見ずに済む。これを Codex へ「import はこの surface だけ・assert は設計から・src 本体を開くな」と渡すと偽 fail ラウンドが減る。RN/Expo モジュール (expo-constants / async-storage / @supabase/supabase-js / react-native / expo-location) は vitest.config.ts の `resolve.alias` でスタブに差し替えれば、stores/api/ws envelope/color/time の純ロジックを **node 環境**で import できる (jest 不要)。zustand は node でそのまま動くので `useStore.getState().apply*()` を直接叩ける。
- **Codex 生成の TS テストは vitest では緑でも `tsc --noEmit` で赤になる罠が 2 形。** omatase mobile (2026-07-23): (1) Codex は export 名に自信が無いと `const fn = (m as any).parseServerEnvelope ?? (m as any).parseEnvelope` と**ハズレ名でヘッジ**する (実行時は当たる方に解決され緑)、(2) 存在しない export を**dead branch**で参照する (`return client.apiFetch(...)` — 実際は `request` が在るので実行されないが tsc が TS2339 で赤)。**vitest run だけで GREEN を出さず必ず `tsc --noEmit` を通す** (タスクが typecheck 緑を要求していなくても)。修正は dead branch/ヘッジの除去で、実装は触らない (これは実装バグでなくテスト作法の欠陥)。
- **「約NaN分」系の壊れ出力は、まず type-valid な入力で再現するか probe してから帰属する。** omatase mobile etaText (2026-07-23): Codex が `etaText({latitude: 35.6}, full)` (**longitude キー欠落**) で "約NaN分" を検出したが、宣言型は `{latitude:number, longitude:number}` = **両キー必須** (null は可・欠落は不可)。probe で type-valid な全形 (from=null / to=null / `{lat:null,lng:null}` / `{lat:num,lng:null}`) を叩くと**全て "-" を返し実装は正しい**。"約NaN分" は型が禁じる欠落キーでしか出ない = 実装バグでなく**テストの過剰仕様 (type-invalid 入力)**。null 座標 (現実に PlanDTO の lat/lng は独立 nullable) の契約を assert し直すと GREEN。残る堅牢性ギャップ (型を迂回して部分 object を渡すと NaN が漏れる) は設計違反でないので 1 行 note に留める。
- **backoff+jitter / AppState 連動 / 再接続時 REST 自己修復が React hook (useEventSocket) に inline されていると unit 不能。** omatase mobile (2026-07-23): jitter 式 `base * (0.8 + Math.random()*0.4)` は hook 本体の中で、純関数として export されていない → WebSocket/AppState/timer をモックして hook を render しない限りテストできない。envelope の parse/encode (WS 層の**テスト可能な部分**) が全緑でも、**再接続ロジックは 1 ミリも検証していない** (role note 43)。判定に「カバーできていない挙動」として明示し、設計/実装の穴 (backoff を pure fn に切り出せば testable) として報告する。envelope 側の「不正封筒の握り潰し」も、parse は throw する契約までしか見えず、実際の握り潰し (hook の try/catch) は未カバー。
- **注入 random で backoff を決定化して検証するとき、cap と jitter の適用順で「上限」の意味が割れる。** omatase web (2026-07-23): `nextBackoff(attempt, random)` は指数 base を 15s に clamp した後で ±20% jitter を掛ける実装。neutral(random=0.5) の plateau は 15000 だが random=1 の plateau は 18000 (>15s) になる。設計「上限15s頭打ち」は plateau=15s (neutral) を指すと読め、jitter 後の絶対上限 15s は別解釈。**絶対上限 `<=15000` を assert すると jitter-after-cap 実装を偽 RED にする** (role note 70)。plateau は neutral で検証し、jitter 幅は非 clamp attempt で ±20% を検証すると両立する。順序ずれは Leader へ「観察」で上げる (jitter-after-cap は再接続 de-sync を steady state でも保つので defensible)。
- **設計docの「現行と同一挙動 (回帰なし)」条項は、*現行がその挙動を実装していない*ことがある。assert する前に merge-base で実測する。** atender 学期カレンダー (2026-07-29): §5.8 C8 が「範囲外 disabled」を現行挙動として列挙していたが、`git show <merge-base>:<component>` を当てて実測すると **merge-base でも disabled 属性は付かず、範囲外の日もマークを描いていた**。設計の C 条項をそのまま assert すると本レーン由来でない pre-existing gap で偽 RED になる。回帰条項の期待値は**設計の文面でなく merge-base の実測**から採り、設計と現行の食い違い自体は別立てで報告する (role note 38 の `git show` 手口が 1 コマンドで効く)。ついでに: RTL でグリフ数を数えるとき `querySelectorAll("span")` + `textContent === "公"` は**グリフ行の親 span も一致して二重計上**する (`children.length === 0` の葉ノードで絞る)。偽 fail の第一容疑者は自分のハーネス (note 35)
- **`xcodebuild test` に環境変数を通すには `TEST_RUNNER_<VAR>` prefix を使う。これで iOS の「JST 固定か端末 TZ 依存か」を非破壊に判別できる。** atender 個人カレンダー (2026-07-29): シミュレータの `TimeZone.current` は既定でホストと同じ `Asia/Tokyo` なので、危険窓テストが緑でも「JST を固定している」証明にならない (端末 TZ 依存でも同じ結果になる)。`TEST_RUNNER_TZ=UTC xcodebuild test ...` を撃つと runner 内 `TimeZone.current` が `GMT` になり (probe で実測)、それでも全 349 緑 = **JST がコードで固定されている**が直接示せた。role note 49 の TZ=UTC 掃引の iOS 版。
- **Swift のテストで `Date(timeIntervalSince1970:)` を手計算で書かない。** 同日 (2026-07-29) に自分でエポックを 1 時間ずれて書き、危険窓のつもりの標本が **JST 22:30 (前日)** になって「実装が UTC 暦で曜日を採っている」という**偽 RED を 1 件出した**。実装は正しかった。`ISO8601DateFormatter` (`.withInternetDateTime, .withFractionalSeconds`) で文字列から起こせば、テスト名と標本がずれない。role note 35「偽 fail の第一容疑者は自分のハーネス」の Swift 版。
- **wire の instant は「同じ時刻か」でなく「serialization の形」まで assert する。** `+09:00` と `Z` は同じ instant だが **zod `.datetime()` は既定で `Z` しか通さない** → クライアントが offset 形式を組むと全 POST/PATCH が 400 になる。`getTime()` 比較のテストは原理的にこれを検出できない。`expect(String(body.start)).toMatch(/Z$/)` の 1 行で捕まる。atender Web の個人予定作成/更新 4 経路がこれで死んでいた (2026-07-29)。詳細 gotcha/zod-datetime-rejects-offset-iso-client-must-send-z.md
- **設計docの §挙動仕様の「例」と §規範の本文が食い違ったら、規範を正としてテストを書き、食い違い自体を報告する。** atender A22 は `DELETE ?scope=all` に originalDate を書いていないが、§4.4 は「繰り返し系列の PATCH / DELETE で必須」と規定しており実装は 400 `ORIGINAL_DATE_REQUIRED` を返す。例に忠実に書くと偽 RED になる。列挙された挙動仕様は**説明の省略**を含むので、パラメータの必須性のような横断規則は §規範側から採る (2026-07-29)。
- **Swift の Codable DTO に memberwise init が無い (`init(from:)` しか候補に出ない) ことがある。実装を読まずに JSON キーを採る手は「自己発見 decode プローブ」。** atender eventkit-export (2026-07-29): `CourseSuspensionDto` / `TimetableSuspensionDto` は `X(zzz:1,...)` を当てても候補が `init(from:)` だけ = memberwise init が使えない (本体で `init(from:)` を書くと合成が抑止される)。テスト用 fixture が作れず詰む。→ **空 `{}` を decode → `DecodingError.keyNotFound(k)` を捕まえて k を足す → `typeMismatch` なら候補型を String/Int/Bool/[]/{} と順に振る、をループする使い捨てテスト 1 本**で全キーと型が 1 ランで採れる (atender では 4 型を同時に採取)。以後は `JSONSerialization` + `JSONDecoder` で fixture を組めば blind を保てる。**逆に memberwise init がある型は `X(zzz: 1, yyy: 2, ...)` と適当なラベルを 5〜6 個並べると `incorrect argument labels in call (have ..., expected 'from:to:hasActiveTimetable:...')` でラベル列が丸ごと出る** (role note 45/55/66 の系譜。1 コンパイルで済む)
- **負のコントロールで「落ちた本数が多い」は成功ではない。** 同日 (2026-07-29) に API3 の JST 境界変異でスコープに無い識別子 (`${from}`) を使い、`ReferenceError` → 全部 500 で 7 本 RED になったのを一瞬「よく効いた」と読みかけた。証明できたのは endpoint に到達していることだけ。**変異には新しい識別子を導入せず、既存の式を算術でずらす** (`fromDay.startOfDay.getTime() + 9*3600*1000`) と 狙った 1 本 (API3) だけが赤くなり、意味的に有効だと分かる。詳細 gotcha/mutation-must-be-proven-to-reach-all-sites.md の追記
- **「承認された初期値」をテストにリテラルで焼くと、その定数を上げたリリースで必ず赤くなる。境界テストは定数を import して `MIN+1` / `MIN` / `MIN-1` から導出する。** atender build 12 (2026-07-29): `MIN_IOS_BUILD` 1→12 で version.test.ts の 4 件が同時陳腐化 (`"ios/2"` `"ios/1"` `"ios/0"` `expect(MIN_IOS_BUILD).toBe(1)`)。設計 §8 の地の文「テスト時は 1」は**環境の説明**であって仕様ではないのに、テストが仕様として焼いていた。相対化した後は「緩めたのでは」を晴らすため **1 変異 = 1 テスト**の対応を示す (`<`→`<=` で #G4 だけ / `< MIN`→`< 1` で #G3 だけ / `!==` で #G2 だけ / route の値 literal 化で #V6 だけ)。将来 bump の耐性は**定数と CFBundleVersion を N+1 にした一時プローブ**で実証し、cp バックアップから `diff` でバイト復元する。詳細 gotcha/policy-constant-literal-in-test-decays-on-bump.md
- **Swift/RN クライアントの wire 契約は、Swift の合成 Codable の意味論を TS 側にモデル化すれば Vitest 内で検証できる。モデルの正しさは 30 秒の `xcrun swiftc` プローブで裏取りする。** atender sync-400 (2026-07-30): iOS `ErrorResponse`(`code`/`message` が非 Optional) が decode できるかを、`keyNotFound`/`valueNotFound`/`typeMismatch`/未知キー無視 の 4 点だけ写した `tests/helpers/swiftDecode.ts` で assert。同じ封筒を実 `swiftc` に食わせると `NEW: OK` / `LEGACY: keyNotFound(code)` で**モデルと完全一致**した (localizedDescription は例の "The data couldn't be read because it is missing." = role note 44)。**「decode できる形か」を `expect(body.error.code).toBe(...)` だけで書くと、TS の `?.` が Swift の非 Optional を再現しないので弱い** — decoder を通し、旧封筒が同じ decoder で throw することも 1 本 (V2-neg) で示すと判別力が証明できる。
- **クライアント encoder の「キー欠落」は、テスト body を TS の型で組んだ瞬間に永久に検出不能になる。逐語 fixture は `JSON.parse` の結果を*そのまま*渡し、「キーが今も無いこと」を `hasOwnProperty` で自己検査する。** atender sync-400 (2026-07-30) で実証: 同じ fixture を `{...e, location: e.location ?? null}` と TS 型で組み直すコピーを作ると、**修正前スキーマでも 200 = 緑のまま**で、逐語版だけが 400 で赤くなった。fixture テストを書いたら必ずこの「組み直し版」を一時的に並走させ、**組み直すと無力化する**ことを見せる (= その fixture の価値の直接証明)。
- **`it.each([[0], [8]])` は要素 1 個の行なので param に `0`/`8` (スカラー) が渡る。配列を渡したいなら `it.each([[[0]], [[8]]])`。** atender の `PATCH daysOfWeek rejects out-of-range value 0` は**数値を送っていて "Expected array, received number" しか踏んでおらず、名前が謳う範囲検証を一度もしていなかった** (0 と 8 の 2 ケースが実質同一テスト)。陳腐化テストの置換ついでに気付いた。テスト名と実際の入力がずれていないかは `it.each` の括弧の数を数える。
- **RTL の `screen.*` は `document.body` 全体を見る。1 つの `it` の中で 2 回 `render()` すると、`queryByLabelText(...)` が**前のレンダの DOM も拾って** null にならない。** atender W5 (2026-07-30) で「範囲外セルが消えていない」という偽 RED を出しかけた。比較用に 2 状態を撮るなら間に `cleanup()` を挟む (`import { cleanup } from "@testing-library/react"`)。role note 35「偽 fail の第一容疑者は自分のハーネス」の RTL 版。
- **負のコントロール用に mock の戻り値 shape を推測で書くと、全ケースが同じ TypeError で落ちて判別力がゼロになる。** atender (2026-07-30): 修正前コンポーネントに `usePersonalEvents` を mock して当てたとき、DTO の必須フィールドを欠いた仮の event を渡して `Cannot read properties of undefined (reading 'map')` で **12/12 が赤** = 「どの assert が本当に効いたか」が消えた。**shape は既存の `tests/lib/*.test.ts` の fixture ヘルパから採る** (そこには本物の DTO が全フィールド書いてある)。正しい shape で撃ち直したら W1/W2/W5/W7/W9 の 5 本だけが赤くなり、非退行の 7 本は緑 = 「設計が変えた挙動だけを見ている」が示せた。
- **オフスクリーンのレイアウト実測 (ImageRenderer + GeometryReader) は、感度コントロール側の条件式を反転させると『壊れた構造でも緑』になり、『計測が無力』と誤診する。** atender の列幅実測 (2026-07-30) で probe の `.frame(minWidth: useEqualColumns ? nil : 0, ...)` を**逆に書き**、修正前構造 (`minWidth` 無し) のはずのケースに `minWidth: 0` が付いて列が揃ってしまった。**感度コントロールが緑のときは、まず probe 側の条件の向きを疑う** (実装でなく自分)。正しく反転させたら legacy 構造は 7 列が親を溢れ、検出できた。
- **設計 doc の「挙動仕様」表の期待値が、同じ doc の規範コード片と算術的に矛盾することがある。** atender §9-A G2 (2026-07-30) は `totalWidth=333, spacing=2` に対し `143/3 x5, 142/3 x2` を期待と書いたが、規範アルゴリズム (`content = totalWidth - spacing*(columns-1)`) で計算すると `138/3 x4, 137/3 x3`。**表の値は totalWidth=345 (= spacing を引く前) の答え**で、著者が content と totalWidth を取り違えていた。同 doc の G4 (`合計 + spacing*6 <= totalWidth`) とも両立しない。→ **規範を正としてテストを書き、doc の意図した数値も別ケースとして両方固定する** (G2 = 345 入力 / G2b = 333 入力)。どちらが赤くなるかで著者の意図と実装のどちらがズレたか分かる。role note 85 の算術版。
- **変異スクリプトで `AND = "&&"` を組んだあと `AND + AND` と連結して 4 文字にし、`literal not found` で 1 変異を無言スキップした。** (2026-07-30)。`block-bash-amp` 回避のための文字列合成は**組んだ直後に `assert src.count(old) == 1`** を置き、0 件なら例外で止める。スキップを print だけにすると「負のコントロール 7 本」と報告した中身が実は 6 本になる。
