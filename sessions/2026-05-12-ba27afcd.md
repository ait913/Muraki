# Session Report 2026-05-12 ba27afcd

## 作業記録

1. ユーザー要望: `apps.aisaba.net` と Aisaba アカウントを含む配下アプリ群のプライバシーポリシーを書いてポートフォリオ Blogs に投稿。
2. `aisaba_platform 2/services` (シンボリックリンク先) と `venv` 内 `nexom/app/auth.py` `parallel_storage.py` を読み、データ取得・保存実態を棚卸し。
   - 中央 `auth.db`: `users` (PBKDF2-HMAC-SHA256 200k iter)、`sessions` (token は SHA-256 ハッシュのみ、平文不保持)、`permission_groups` / `_members`
   - ユーザー個別 DB: `profiles` / `install_apps` / `shortcut_apps`
   - `parallel_storage`: アップロードファイルのメタ
   - 各アプリのテーブル (`calender` の `rooms` / `events` 等)
   - アクセスログは IP / method / path / status / dt_ms / UA
3. `aisaba_main/templates/portfolio.html` と `nexom_web/templates/base.html` に GA4 (`G-YHVGSJZTKS`) を発見。`apps.aisaba.net` / `user.aisaba.net` / `aisaba_user` templates には無し。ポリシー本文では「対象外だが関連サイトとして明記」する形に。
4. `portfolio_manager` の `pm_api.upload_works` 仕様を読み、`project_id=""` で新規作成、`types="blogs"`、`is_public=True` で投稿できることを確認。Markdown 本文は `description` フィールド。
5. chrome-devtools MCP で `apps.aisaba.net/portfolio_manager` を開き、`load-contents` で既存 5件 (運営者情報など) のフォーマットを参照 (date は `2026.05.10` 形式 / tags はカンマ区切り / 連絡先は `touri.development@gmail.com`)。
6. `evaluate_script` から `fetch /portfolio_manager/api/upload-works/` を叩いて投稿 → `{"ok":true}` 受領、`project_id=cQyYtQh` で登録 (blogs 件数 5→6)。
7. 公開側 `https://aisaba.net/blogs/cQyYtQh` を開き、h1 / 13節の H3 / H4 サブ / inline code (40箇所) / Cloudflare・mailto・X リンクが正しくレンダリングされることを確認。

## 課題点

- ~~~
  - **何が課題か**: ディレクトリのシンボリックリンク先 (`Developments/aisaba_platform`) の実体が `Projects/aisaba_platform 2` (スペース入り) で、初回 `ls services/` が無音失敗 (exit 1) し、原因特定に2-3 手を費やした。
  - **いつ起こるか**: aisaba_platform 配下のファイルを Bash 経路で読みに行く度。
  - **次どうするか**: 最初に `ls -la` で symlink の解決先を確認する。スペース入り Projects ディレクトリへ直接アクセスする方が確実。

- ~~~
  - **何が課題か**: 投稿先パス (`apps.aisaba.net/portfolio_manager`) は知っていたが、公開表示が `aisaba.net/blogs/<project_id>` であることは Routing を読むまで分からなかった。確認時に1度別ページを踏みかねなかった。
  - **いつ起こるか**: portfolio_manager 投稿後の表示確認。
  - **次どうするか**: 公開表示パスは `aisaba_main/router.py` の `Get("blogs/{project_id}", ...)` を覚えておく。

## いい点

- ~~~
  - **何が良かったか**: パネル UI を踏まず `evaluate_script` から直接 `upload-works` API を叩いた。HTML 構造を解析せずに済み、本文の長文も JSON で一発投入できた。
  - **いつ効いたか**: 3,400 文字超の Markdown 本文を投稿する場面で、UI 入力フィールドのチャタリングや IME 揺らぎを完全に回避できた。
  - **どう続けるか**: portfolio_manager のような自前 SPA でも、API シグネチャが既知なら fetch 直叩きを第一選択にする。UI 操作はフォーム特有のバリデーション挙動を見たい時だけにする。

- ~~~
  - **何が良かったか**: ポリシー本文を「実装の事実」だけに絞り、規約っぽい言い回しを避けた。`PBKDF2-HMAC-SHA256 200,000 iter` のような具体値で書き、`users` / `sessions` テーブル定義をそのまま開示している。
  - **いつ効いたか**: Touri 文体 (マーケ語禁止、動機ベース、コード片中心) に合致したと判断。Sycophancy 禁止の方針とも整合する。
  - **どう続けるか**: 規約系を書く時は「現存するスキーマ / 実装」を一次資料として、抽象的な権利語より具体名 + 行動可能な記述を優先する。

## ナレッジ

- 追加・更新なし。既存の `knowledge/library/aisaba-stack.md` で auth / parallel_storage / 各サービスの構造は把握できており、今回の知見はその範囲内。`portfolio_manager` の `upload-works` API シグネチャ (`project_id` / `types` / `project` / `tags` / `date` / `header` / `images` / `description` / `is_public`) は将来的に同種の Blogs 投稿が複数発生したら `library/aisaba-stack.md` に追記する価値あり (今回1回限りなので保留)。
