# Session Report 2026-06-29 73e63ced

dandan-app（チーム開発タスクをリモートMCPで半自動化）を構想合わせ→設計→実装→本番デプロイ→実プロトコル検証まで一気通貫。

## 作業記録

1. 前身 `n-wasabi/dandan-mcp`（天野作・ローカルstdio版・Go・LLMなしホスト委譲）をキャッチアップ調査。MVP完成度は高いと判定。
2. Touri のビジョンを段階的に確認 → 「リモートMCP(URLで繋ぐ) + GitHub OAuth + プロジェクト=リポ単位 + 進捗ダッシュボード」。
3. ホスト対象を **agent型のみ(Claude Code/Codex)** に確定（chat型は iframe 非描画で除外）。サーバーはコード読まず、推論はホストがローカルで。
4. 認証方式を検討 → Research で **OAuth App は Issues 限定不可**（private は `repo` フルスコープ強制）判明 → **GitHub App** 確定。
5. 新規リポ `n-wasabi/dandan-app`(private) 作成 + Muraki PJ化(CLAUDE.md/scaffold)。
6. Pre-design Research（リモートMCP+OAuthホスト対応/MCP認可spec(RFC9728/8414/8707)/MCP Prompts/Coolify）→ Claude Code・Codex 両対応確認。Codex は prompts 非surface → ツール戻り値ステアリング主経路に。
7. Architect 基盤設計doc 初版（マルチテナント+ダッシュボード+Postgres）→ **Touri がスコープ縮小**（DB/ダッシュボード削除）→ **ステートレス版に全面圧縮**（GitHub=真実、状態はagentコンテキスト、単一 `/mcp`、偏りライブ計算）。
8. 設計原則「**床を上げ、天井は上げっぱなしにする**」(判断=モデル/事実=コード) を注入。
9. 承認ゲート → main コミット。Slice1「認証背骨」(自前AS/自己完結JWT/ping/whoami) 実装 → Reviewer GREEN(20) → merge。
10. Slice2「ドメインツール」(get_breakdown_rubric/get_assignment_context/preview_balance/preflight_github/create_issues) 実装 → install URL の穴を差し戻し修正 → Reviewer GREEN(30) → merge。
11. Coolify デプロイ: deploy key は n-wasabi org ポリシーで不可 → **GitHub App ソース(UI)** → `POST /applications/private-github-app` で app 作成(environment_uuid込) → env投入 → deploy 成功。
12. redirect ループ発生 → `redirect:"both"` + **fqdn 削除→戻し** で解消 → /healthz 200・/mcp 401・OAuth metadata 実機確認。
13. appily SKILL を **全APIエンドポイント網羅**に拡充 + knowledge/tool-quirk/coolify-api.md に癖6件追記。
14. **自前mint token(署名鍵既知) + go-sdk client で実MCPプロトコル検証** → 全7ツール健全・配列パラメータ正常。
15. Touri が omatase-demo でドッグフーディング中に **submit_plan 幻覚** 発見（新dandanに submit_plan は無い＝agentの誤呼び）→ rubric に「submit_plan 不在」ガード追加 → 再デプロイ。

## 課題点

- 初版設計が過剰（マルチテナント+DB+ダッシュボード）。Touri のスコープ縮小で救われた。
  - **何が課題か**: MVP スコープが固まりきる前に Architect に投げた
  - **いつ起こるか**: 要件確認より先に「作れる形」を設計させた時
  - **次どうするか**: 承認ゲート前に Leader が「最小/ステートレスで閉じるか」の問いを立ててから Architect 召集
- テストが in-process 型渡しで、実ホストの JSON schema 経路(配列マーシャリング)を検証してなかった。
  - **何が課題か**: Reviewer GREEN でも実ホスト経路の穴(配列文字列化疑い)は残った
  - **いつ起こるか**: MCP ツールを in-process ClientSession だけでテストした時
  - **次どうするか**: 重要ツールは自前mint token + go-sdk client の**実プロトコルスモーク**を標準化（本セッションで手法確立済）
- ステートレス設計が agent の直感(取り込みステップ)に反し submit_plan 幻覚を招いた。
  - **何が課題か**: 「agent が何を期待するか」視点が設計時に抜けた
  - **いつ起こるか**: 慣習的なツール名(submit_*)を消した時
  - **次どうするか**: 消したツールは rubric/description で明示的に「存在しない」と先回り
- 秘密(client secret)が chat 露出。UI投入を勧めたが結局 API 投入に。
  - **次どうするか**: 露出後は regenerate を必須TODO化。最初から Coolify UI 投入を強制する導線

## いい点

- **スコープ縮小(Touri主導)** で DB/ダッシュボード削除 → プロダクトが締まった。
  - **どう続けるか**: Leader も「これ要る?」を早期に問う。generic best practice より最小で閉じる形を優先
- **「床を上げ天井上げっぱなし」原則** が設計全体を貫いた(判断=モデル/事実=コード、観点テンプレのheadroom、非破壊再抽出)。
  - **どう続けるか**: LLM オーケストレーション設計の背骨として再利用
- **自前mint token で実プロトコル検証** — OAuth ブラウザ無しで本番サーバを実経路検証できた。
  - **どう続けるか**: リモートMCP の検証標準手法に
- knowledge/gotcha(coolify redirect loop / traefik stale label) が実際に効き復旧が速かった。
  - **どう続けるか**: 詰まり時は既存 knowledge を先に grep

## ナレッジ

- `knowledge/library/remote-mcp-oauth-hosts-2026.md` — リモートMCP+OAuthのClaude Code/Codex対応・MCP認可spec(Researcher)
- `knowledge/pattern/remote-mcp-multitenant-self-as.md` — 自前AS+マルチテナント(Architect初版。※ステートレス化で一部陳腐化、要見直し)
- `knowledge/tool-quirk/coolify-api.md` — env重複/description許可文字/environment_uuid必須/github-appソースAPI/deploy key無効 等の癖を追記
- `~/.claude/skills/appily/SKILL.md` — 全エンドポイント・カタログ + private repo デプロイ完全フローを追加
- `projects/dandan-app/CLAUDE.md` — 確定制約の真実(ステートレス/GitHub App/床天井原則/Coolify構成)
- `memory/project_dandan_app.md` — PJポインタ(本番稼働 dandan.appily.run)
